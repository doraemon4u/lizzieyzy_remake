<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>featurecat</groupId>
	<artifactId>lizzie</artifactId>
	<version>yzy2.5.3</version>

	<properties>
		<!-- Prevent warning: "[WARNING] Using platform encoding (UTF-8 actually) 
			to copy filtered resources, i.e. build is platform dependent!" -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
	</properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<!-- Phase 2: build to Java 17 bytecode (LTS) to pair with JCEF 135 and
					     unlock JDK 17 runtime features. JDK 21/26 can still compile this. -->
					<release>17</release>
					<compilerArgs>
						<arg>-Xlint:all</arg>
					</compilerArgs>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.0.2</version>
				<!-- No need to specify the main class since maven-shade-plugin will 
					add it. -->
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.6.0</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<transformers>
								<transformer
									implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<mainClass>featurecat.lizzie.Lizzie</mainClass>
								</transformer>
							</transformers>
							<shadedArtifactAttached>true</shadedArtifactAttached>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.2.5</version>
				<configuration>
					<!-- Required to get the test failure reports in stdout -->
					<useFile>false</useFile>
				</configuration>
			</plugin>
			<plugin>
				<groupId>com.coveo</groupId>
				<artifactId>fmt-maven-plugin</artifactId>
				<version>2.5.1</version>
				<executions>
					<execution>
						<goals>
							<goal>format</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

		</plugins>
		<pluginManagement>
			<plugins>
				<!--This plugin's configuration is used to store Eclipse m2e settings 
					only. It has no influence on the Maven build itself. -->
				<plugin>
					<groupId>org.eclipse.m2e</groupId>
					<artifactId>lifecycle-mapping</artifactId>
					<version>1.0.0</version>
					<configuration>
						<lifecycleMappingMetadata>
							<pluginExecutions>
								<pluginExecution>
									<pluginExecutionFilter>
										<groupId>com.coveo</groupId>
										<artifactId>
											fmt-maven-plugin
										</artifactId>
										<versionRange>
											[2.5.1,)
										</versionRange>
										<goals>
											<goal>format</goal>
										</goals>
									</pluginExecutionFilter>
									<action>
										<ignore></ignore>
									</action>
								</pluginExecution>
							</pluginExecutions>
						</lifecycleMappingMetadata>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<dependencies>

		<!-- https://mvnrepository.com/artifact/org.json/json -->
		<dependency>
			<groupId>org.json</groupId>
			<artifactId>json</artifactId>
			<version>20180130</version>
		</dependency>

		<!-- Phase 2: JCEF upgraded from 95 (2021, no arm64) to 135 (chromium-135). -->
		<dependency>
			<groupId>me.friwi</groupId>
			<artifactId>jcefmaven</artifactId>
			<version>135.0.20</version>
		</dependency>

		<!-- JCEF natives are per-platform (see <profiles> below). Bundle only the local
		     platform's Chromium via e.g. -Pnative-macosx-arm64 to keep the fat jar small.
		     Without a profile jcefmaven downloads the current platform's natives on first run. -->

		<!-- Phase 2: maintained JSch fork replaces deprecated ganymed-ssh2 (2011, ssh-rsa
			 rejected by modern OpenSSH). com.github.mwiede:jsch supports modern key algs. -->
		<dependency>
			<groupId>com.github.mwiede</groupId>
			<artifactId>jsch</artifactId>
			<version>2.28.6</version>
		</dependency>

		<!-- https://mvnrepository.com/artifact/org.swinglabs.swingx/swingx-core -->
		<dependency>
			<groupId>org.swinglabs.swingx</groupId>
			<artifactId>swingx-core</artifactId>
			<version>1.6.4</version>
		</dependency>

		<!-- https://code.google.com/archive/p/juniversalchardet -->
		<dependency>
			<groupId>com.googlecode.juniversalchardet</groupId>
			<artifactId>juniversalchardet</artifactId>
			<version>1.0.3</version>
		</dependency>

		<!-- https://mvnrepository.com/artifact/com.jhlabs/filters -->
		<dependency>
			<groupId>com.jhlabs</groupId>
			<artifactId>filters</artifactId>
			<version>2.0.235</version>
		</dependency>

		<dependency>
			<groupId>org.java-websocket</groupId>
			<artifactId>Java-WebSocket</artifactId>
			<version>1.5.0</version>
		</dependency>

		<dependency>
			<groupId>io.socket</groupId>
			<artifactId>socket.io-client</artifactId>
			<version>1.0.0</version>
		</dependency>

	</dependencies>

	<!-- Per-platform JCEF natives profiles: build with e.g. -Pnative-macosx-arm64 to bundle only
	     the local Chrome/CEF natives into the fat jar (offline-capable). Without a profile,
	     jcefmaven downloads the current platform's natives automatically on the first launch. -->
	<profiles>
		<profile>
			<id>native-windows-amd64</id>
			<dependencies>
				<dependency>
					<groupId>me.friwi</groupId>
					<artifactId>jcef-natives-windows-amd64</artifactId>
					<version>jcef-ca49ada+cef-135.0.20+ge7de5c3+chromium-135.0.7049.85</version>
				</dependency>
			</dependencies>
		</profile>
		<profile>
			<id>native-linux-amd64</id>
			<dependencies>
				<dependency>
					<groupId>me.friwi</groupId>
					<artifactId>jcef-natives-linux-amd64</artifactId>
					<version>jcef-ca49ada+cef-135.0.20+ge7de5c3+chromium-135.0.7049.85</version>
				</dependency>
			</dependencies>
		</profile>
		<profile>
			<id>native-macosx-amd64</id>
			<dependencies>
				<dependency>
					<groupId>me.friwi</groupId>
					<artifactId>jcef-natives-macosx-amd64</artifactId>
					<version>jcef-ca49ada+cef-135.0.20+ge7de5c3+chromium-135.0.7049.85</version>
				</dependency>
			</dependencies>
		</profile>
		<profile>
			<id>native-macosx-arm64</id>
			<dependencies>
				<dependency>
					<groupId>me.friwi</groupId>
					<artifactId>jcef-natives-macosx-arm64</artifactId>
					<version>jcef-ca49ada+cef-135.0.20+ge7de5c3+chromium-135.0.7049.85</version>
				</dependency>
			</dependencies>
		</profile>
	</profiles>
</project>
